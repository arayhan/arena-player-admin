# `src/domain/`

This directory is **read-only in this repo**. It is a byte-identical copy of `arena-player-web/src/domain/`, verified by `pnpm check:domain`. Drift is repaired by fixing `arena-player-web` and re-copying — never by editing the files here.
