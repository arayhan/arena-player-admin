.next/
node_modules/
pnpm-lock.yaml

# Parallel-feature checkouts — see docs/rules/worktrees.md. Every ignore in
# this file is anchored at the repo root, so a worktree's copy of an ignored
# path is not ignored: without this line `format:check` fails on
# .worktrees/*/docs/DESIGN.html.
.worktrees/

# Reference artifact, hand-authored, not production. Reformatting it would
# produce a large meaningless diff.
docs/DESIGN.html

# Closed phases. History records what was true when the work ran and is
# never rewritten, whitespace included. docs/PROGRESS.md is deliberately NOT
# here: it is appended every step, so keeping it formatted costs nothing.
docs/progress-archive/
